<Window x:Class="WPF_ChunChen_CRM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_ChunChen_CRM"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Button Content="查询" HorizontalAlignment="Left" Margin="331,55,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
        <DataGrid Name="dataGrid" HorizontalAlignment="Left" Height="214" Margin="151,124,0,0" VerticalAlignment="Top" Width="500" >
            <!--<DataGrid.Columns>
                <DataGridTextColumn Header = "唯一标示符" Width = "100" Binding="{Binding Id}" />
                <DataGridTextColumn Header = "员工编号" Width = "100" Binding="{Binding EmployeeNo}" />
                <DataGridTextColumn Header = "姓名" Width = "100" Binding="{Binding Name}" />
                <DataGridTextColumn Header = "性别" Width = "100" Binding="{Binding Gender}" />
                <DataGridTextColumn Header = "生日" Width = "100" Binding="{Binding Birthday}" />
            </DataGrid.Columns>-->

        </DataGrid>

    </Grid>
</Window>
